{
  "name": "product-workspace",
  "private": true,
  "scripts": {
    "start": "cmd /c \"cd productdetail && set PORT=3002 && npm start\"",
    "test": "npm --prefix productdetail run test -- --watch=false",
    "build": "npm --prefix productdetail run build"
  }
}
